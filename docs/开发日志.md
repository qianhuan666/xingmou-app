# 星眸训练台安卓端开发日志

## 2026-09-23 阶段 0 工程脚手架

### 本次目标

完成 Android 工程最小可编译结构，为后续 Room、Compose 三端口和 Agent Runtime 开发建立统一入口。

### 已完成

- 创建根工程 `settings.gradle.kts` 和根 `build.gradle.kts`。
- 创建 `app` Android Application 模块。
- 配置 AGP、Kotlin、Compose Compiler、KSP 和 Gradle Wrapper。
- 配置 `minSdk 26`、`compileSdk 36`、`targetSdk 36`。
- 配置 Compose、Room、OkHttp、Gson、Coroutines 依赖。
- 创建 `AndroidManifest.xml`、应用主题和字符串资源。
- 创建 `com.xingmou.MainActivity`，实现阶段 0 占位页面。
- 保留现有 `core` Kotlin 规则、LLM、模型和安全骨架。
- 增加 `local.properties` 的本机 Android SDK 路径；该文件已被 Git 忽略。

### 阶段 0 验收标准

| 验收项 | 状态 |
|---|---|
| Gradle 工程文件存在 | 已完成 |
| Android Manifest 存在 | 已完成 |
| MainActivity 存在 | 已完成 |
| Compose 基础页面存在 | 已完成 |
| `./gradlew assembleDebug` | 已通过 |
| 平板模拟器启动 | 本机暂无 AVD 或在线设备，待 Android Studio 创建平板模拟器后执行 |

### 当前阻塞或注意事项

- 本机没有系统级 Gradle 命令，项目使用 Gradle Wrapper。
- 首次构建需要下载 Gradle 8.9、Android Gradle Plugin 和 Maven 依赖。
- 首次构建已自动安装本机缺少的 Android SDK Build-Tools 34 和 Android SDK Platform 36。
- 首次编译发现 `JsonValidator.kt` 使用了 Gson `JsonArray.isNotEmpty()`，已改为兼容写法 `speech.size() > 0`。
- 第二次执行 `gradlew.bat assembleDebug` 成功，生成 `app/build/outputs/apk/debug/app-debug.apk`，文件大小约 10 MB。
- 当前 `emulator -list-avds` 没有返回设备，`adb devices` 也没有连接设备，因此尚未执行安装和启动验收。
- 当前页面只是工程启动占位，不代表阶段 4 UI 已开始。
- Room Entity、DAO、Agent Runtime 和三端口页面仍属于后续阶段。

### 下一步

1. 在 Android Studio 创建平板 AVD，并安装当前 Debug APK 做启动验收。
2. 进入阶段 1，建立 Room 数据库和 DAO。
3. 进入阶段 2，补齐知识检索、训练引擎、分析引擎和方案状态机。
4. 在阶段 2.5 建立 Agent Runtime、工具白名单和执行状态机。

## 2026-09-23 阶段 1 Room 数据层

### 本次目标

建立本地 Room 数据库、核心 Entity、DAO 和可重复初始化的知识/任务种子，为后续领域层和 Agent Runtime 提供持久化基础。

### 已完成

- 创建 `QizhiDatabase` Room 数据库单例，数据库文件名为 `qizhi_training.db`。
- 创建儿童档案、训练记录、能力画像、来源、判断、知识条目、风险标记、方案版本、对话消息和知情同意 Entity。
- 创建 Agent 运行、步骤、工具调用、记忆、审核请求和决策追踪 Entity。
- 为儿童、训练记录、知识库、风险标记、方案、对话和 Agent 运行建立 DAO。
- 加入 3 条已审核本地知识种子：安全暂停、最小辅助原则、家庭短时高频练习。
- 加入 8 条阶段 1 白名单训练任务种子。
- 在 `MainActivity` 启动时打开数据库并异步触发种子写入。
- 新增 `SeedDataTest`，验证知识条目审核状态和训练任务数量。

### 验收结果

| 验收项 | 状态 |
|---|---|
| Room Entity 编译和 KSP 生成 | 已通过 |
| DAO 编译 | 已通过 |
| 数据库单例编译 | 已通过 |
| 知识/任务种子测试 | 已通过 |
| `testDebugUnitTest` | 已通过 |
| `assembleDebug` | 已通过 |

### 说明

- 本阶段只完成数据结构和种子初始化，不代表 UI 已接入完整数据流。
- `SeedDatabaseCallback` 保留为轻量扩展点；当前由 `DatabaseSeeder` 在数据库单例创建后执行异步种子写入。
- 当前数据库版本为 1，后续修改 Entity 必须增加 Room migration，不应直接删除用户数据。

### 下一步

1. 进入阶段 2，先实现 `KnowledgeRetriever`、`TrainingEngine`、`PlanStateMachine` 和 `AnalysisEngine`。
2. 为 DAO 增加内存数据库测试和数据权限测试。
3. 将 `TaskWhitelist` 改为从 Room 种子/审核内容读取，同时保留确定性代码兜底。
4. 进入阶段 2.5，接入 Agent Runtime 的 `ToolRegistry` 和运行轨迹表。

## 2026-09-23 阶段 2 领域与规则层

### 本次目标

在 Room 数据层之上建立确定性的风险路由、知识检索、训练任务控制、方案审核状态机和训练过程分析能力，为后续 Agent Runtime 提供可调用的安全边界。

### 已完成

- 扩展 `RiskEngine`，覆盖自伤、攻击、抽搐/癫痫、吞咽/呼吸困难、严重跌倒、意识异常、明显发育倒退、走失/陌生人风险、疑似急症 9 类风险。
- 增加结构化 `RiskAssessment`，保留原有 `assess()` 兼容入口，并实现安全停止优先于暂停、否定表达过滤和风险类型记录。
- 新增 `KnowledgeRetriever`：只检索已审核且启用的知识条目；总数最多 3 条、同领域最多 2 条；支持澄清、未找到、转介和安全停止路由。
- 新增 `TrainingEngine`：校验任务白名单、限制难度 1–5、控制单次最多调整 ±1 级、独立调整支持等级，并支持映射为 `TrainingRecordEntity`。
- 新增 `PlanStateMachine`：严格约束 `DRAFT → CONFIRMED → ACTIVE`，模型不能直接激活，退回/替代/归档必须由专业人员并填写理由。
- 新增 `AnalysisEngine`：计算正确率、首次正确率、平均反应时间、提示等级、独立完成率和趋势；有效记录少于 3 条不判断趋势，下降信号触发人工复核。
- 将 `DifficultyController` 难度范围从 1–10 收紧为 1–5。
- 新增阶段 2 单元测试，覆盖 9 类风险、暂停优先级、否定词、知识数量上限、任务白名单、难度边界、人工审核状态机和趋势复核。

### 验收结果

| 项目 | 状态 |
|---|---|
| 风险红队测试 | 已通过 |
| KnowledgeRetriever 测试 | 已通过 |
| TrainingEngine 测试 | 已通过 |
| PlanStateMachine 测试 | 已通过 |
| AnalysisEngine 测试 | 已通过 |
| `testDebugUnitTest` | 已通过（现有及新增测试全部通过） |
| `assembleDebug` | 已通过 |

### 说明

- 本阶段只完成领域/规则层，不包含 Agent Runtime、工具注册表或执行状态机；这些内容安排在阶段 2.5。
- 构建仍会提示 AGP 8.7.3 尚未正式针对 compileSdk 36，但不影响当前测试和 APK 构建结果。

### 下一步

进入阶段 2.5：实现 Agent Runtime、工具注册表、工具授权/幂等校验、运行状态机，以及将决策追踪写入阶段 1 已建好的 Agent 数据表。

## 2026-09-23 阶段 2.5 Agent Runtime 基础

### 本次目标

建立可被三端口和后续模型编排调用的最小 Agent Runtime，先固定工具白名单、端口授权、幂等调用和状态流转，避免模型直接执行高风险副作用。

### 已完成

- 新增 `ToolRegistry`：登记风险评估、知识检索、训练记录、过程分析和方案相关工具。
- 增加工具端口白名单、工具风险等级、人工审批要求和幂等键校验。
- 新增 `ExecutionStateMachine`：约束 `CREATED → ROUTING → PLANNING → EXECUTING_TOOL / WAITING_APPROVAL → VALIDATING → COMPLETED`，并支持暂停、失败和安全停止分支。
- 新增 `AgentRuntime`：管理运行快照、工具执行、幂等缓存、工具调用轨迹和状态步骤轨迹。
- 提供 `AgentRunEntity`、`AgentStepEntity`、`ToolCallEntity` 映射，能够写入阶段 1 已建立的 Room 审计表。
- 高风险工具（如方案激活）必须进入人工审批；安全停止状态禁止继续调用工具。
- 新增阶段 2.5 单元测试，覆盖未知工具、端口越权、安全停止、人工审批、越级状态、幂等调用和审计记录。

### 验收结果

| 项目 | 状态 |
|---|---|
| ToolRegistry 授权测试 | 已通过 |
| Agent 状态机测试 | 已通过 |
| 幂等调用测试 | 已通过 |
| 安全停止测试 | 已通过 |
| `testDebugUnitTest` | 已通过（13 项测试） |
| `assembleDebug` | 已通过 |

### 边界说明

- 当前 Runtime 不直接负责大模型推理、网络请求或 UI 展示；这些能力通过后续注入的 `ToolHandler` 和 Compose ViewModel 接入。
- 当前仅完成工具层基础授权，真实的专业审核页面和方案版本持久化流程安排在后续阶段。

### 下一步

进入阶段 3：联调 ContextAssembler、模型输出校验、AgentOrchestrator、记忆与决策追踪；三端口 Compose 页面按计划在阶段 4 实现。

## 2026-09-23 阶段 3 AI 层联调

### 本次目标

按照开发计划完成三端口 AI 闭环的离线联调：风险检查、上下文组装、模型调用、结构校验、工具执行、人工审核与确定性降级。Compose 三端口 UI 仍按计划留在阶段 4。

### 已完成

- 新增 `ContextAssembler`：按端口组装最小必要上下文，注入训练记录摘要和已审核知识 ID，并在模型调用前执行风险检查。
- 加固 `PromptBuilder`：儿童化名、年龄段、任务、近期摘要和风险标记均在注入前执行脱敏。
- 新增 `AgentOrchestrator`：打通“风险检查 → 上下文 → 模型 JSON → 校验 → 工具执行/人工审核 → 完成或降级”的闭环。
- 新增 `ModelGateway` 抽象与 `DeepSeekModelGateway` 适配器，单元测试可使用 Mock 模型，真实环境可接现有 `DeepSeekClient`。
- 新增 `DeepSeekClientFactory`，从未纳入 Git 的 `local.properties` 或环境变量读取开发密钥；未配置时禁用真实网络模型。
- 扩展 `JsonValidator`：增加 Agent `action`、工具参数对象、幂等键和 `requires_review` 的结构校验。
- 将 `PortGuard` 接入模型输出校验，儿童端和家长端发现越权词时不允许输出进入界面；标准免责声明不误判。
- 新增 `MemoryManager`：只保存脱敏且具备观察/报告依据的记忆，支持撤回及 Room Entity 映射。
- 新增 `DecisionTraceFactory`：统一记录规则、知识、工具和人工决定的证据链。
- 高风险文本在调用模型和工具之前直接进入确定性安全响应。
- 模型超时、非法 JSON、短句超长、未知/越权工具等情况进入固定降级路径。
- Agent 循环支持工具结果回传模型和连续工具调用，并通过 `maxSteps`（1–12）限制防止无限循环。

### 验收结果

| 项目 | 状态 |
|---|---|
| 儿童端 Mock Agent 闭环 | 已通过 |
| 家长端知识工具闭环 | 已通过 |
| 连续两次工具调用与结果回传 | 已通过 |
| 专业端方案人工审批门 | 已通过 |
| 高风险前置拦截 | 已通过 |
| 非法模型输出降级 | 已通过 |
| 上下文隐私脱敏 | 已通过 |
| 记忆保存与撤回 | 已通过 |
| 决策证据链 | 已通过 |
| `testDebugUnitTest` | 已通过（18 项测试） |
| `assembleDebug` | 已通过 |

### 当前边界

- 当前使用 Mock 模型完成自动化联调，尚未配置真实 DeepSeek API Key，因此没有发送真实外部请求。
- `AgentOrchestrator` 已具备 DeepSeek 适配入口；API Key 仍须从本地安全配置注入，禁止硬编码或提交 Git。
- 本阶段未提前实现阶段 4 的 Compose 三端口正式 UI。

### 下一步

进入阶段 3.5：实现训练完成、连续失败、风险检测和专业审核等事件闭环；随后进入阶段 4 开发三端口 Compose 页面。

## 2026-09-23 阶段 3.5 Agent 事件闭环与人机协同

### 本次目标

让 Agent 能够响应训练、风险、记录阈值、家长观察、审核和恢复事件，并把事件及其确定性处理结果写入本地审计链路。

### 已完成

- 新增 `AgentEvent` 事件模型，覆盖 `UserMessage`、`TrainingCompleted`、`ConsecutiveFailures`、`RiskDetected`、`RecordsThresholdReached`、`ParentObservationAdded`、`PlanRejected`、`ReviewApproved` 和 `SessionResumed`。
- 新增 `AgentEventProcessor`，将事件确定性路由到完成、暂停、安全停止、规划或等待审核状态。
- 儿童训练完成事件可生成 `TrainingRecordEntity`，并根据近期结果分别调整难度和支持等级。
- 连续失败、哭闹或疲劳事件进入暂停；高风险事件生成脱敏后的 `SafetyFlagEntity`，且不得继续训练。
- 达到 3 条有效记录时触发过程分析入口；数据不足时返回固定信息不足响应。
- 专业审核严格分为 `draft → confirmed → active` 两步；直接从草案激活会被拒绝。
- 方案退回必须填写理由，并同步更新方案版本和审核请求状态。
- 新增 `AgentEventCoordinator` 与 `RoomAgentEventStore`，在 Room 事务中保存事件、训练记录、风险标记、方案和审核结果。
- 新增 `AgentEventEntity` 和事件查询 DAO；Room 数据库升级到版本 2，并提供 1→2 无损迁移。
- `AgentRuntime` 支持从 Room 的运行、步骤和工具记录恢复，并重建幂等缓存，避免进程恢复后重复写入。
- 新增阶段 3.5 单元测试，覆盖儿童、家长、专业审核、暂停恢复、数据库记录映射和恢复幂等。

### 验收结果

| 项目 | 状态 |
|---|---|
| 儿童训练完成事件 | 已通过 |
| 连续失败暂停 | 已通过 |
| 风险事件与脱敏标记 | 已通过 |
| 记录阈值触发 | 已通过 |
| 方案确认与激活两步审核 | 已通过 |
| 方案退回及理由校验 | 已通过 |
| 会话恢复 | 已通过 |
| 恢复后幂等防重复执行 | 已通过 |
| Room 1→2 迁移编译 | 已通过 |
| `testDebugUnitTest` | 已通过（24 项测试） |
| `assembleDebug` | 已通过 |

### 当前边界

- 事件闭环已具备领域逻辑和 Room 持久化接口，尚未由 Compose 页面实际触发。
- Room 迁移已加入正式构建，但设备端升级验证需在模拟器安装旧版数据库后执行一次升级测试。

### 下一步

进入阶段 4：实现儿童端、家长端和专业端 Compose 页面及 ViewModel，把事件协调器、AgentOrchestrator 和 Room 数据流接入实际交互。

## 2026-09-23 阶段 4 三端口 Compose 工作台

### 本次目标

实现儿童端、家长端和专业端正式页面，并通过统一 ViewModel 接入 Room、AgentOrchestrator 和事件协调器，形成可在模拟器中完整操作的阶段 4 工作台。

### 已完成

- 新增 `XingmouViewModel` 和统一 `XingmouUiState`，管理端口切换、训练、知识检索、过程分析、方案审核和 Agent 状态。
- 儿童端实现大触控目标、单步短指令、难度/支持等级展示、“先休息”常驻操作、暂停恢复和安全停止状态。
- 儿童训练选择通过 `TrainingCompletedEvent` 写入 Room；连续失败和主动休息通过事件协调器进入暂停状态。
- 家长端实现观察输入、风险路由、已审核本地知识检索、建议来源、训练记录数量和固定免责声明。
- 专业端实现记录数量与数据充分性、过程分析、警示信号、方案草案、审核意见和 Agent 运行信息。
- 专业方案严格执行 `draft → confirmed → active` 两步审核；退回修改必须填写理由。
- `AgentOrchestrator` 已接入 ViewModel，当前采用确定性的本地安全 ModelGateway 做离线闭环，不会因未配置 Key 自动发送网络请求。
- 新增响应式工作台：宽屏使用 N3 侧栏，窄屏使用顶部端口选择器；三端页面均支持纵向滚动。
- 新增 Coral 暖色 Compose 主题、4dp 间距体系、公共信息表面和 Compose 自绘“小星”标记。
- 新增 Hallmark 设计记录与可移植 `tokens.css`，保留既有蓝色作为信息色。
- 新增 `UiStateReducerTest`，覆盖儿童完成/暂停/安全停止状态，以及专业方案两步生效和退回理由约束。

### 验收结果

| 项目 | 状态 |
|---|---|
| 三端口 Compose 编译 | 已通过 |
| Room 训练记录写入路径 | 已接入 |
| Agent 事件协调路径 | 已接入 |
| AgentOrchestrator 离线闭环 | 已接入 |
| 儿童暂停与安全状态 | 已实现 |
| 家长知识来源展示 | 已实现 |
| 专业两步审核 | 已实现 |
| `testDebugUnitTest` | 已通过（28 项测试） |
| `assembleDebug` | 已通过 |

### 设计与安全说明

- 采用 Workbench 工作台结构和 Coral 暖色主题，儿童端低刺激、家长端清晰、专业端提高信息密度。
- 不展示儿童分数和排名，不用颜色单独表达风险；风险状态同时提供明确文字。
- 儿童端触控目标不低于 64dp，家长与专业端主要操作不低于 48dp。
- 当前本地安全 ModelGateway 只用于验证 UI 到 AgentOrchestrator 的完整调用链；真实 DeepSeek 网关切换需单独完成隐私与网络验收。

### 下一步

1. 在 Android Emulator 平板 AVD 安装并启动 Debug APK，完成横竖屏与三端主流程人工验收。
2. 增加 Compose UI 自动化测试和 Room 仪器测试。
3. 在明确的数据出境与密钥保护方案后，接入真实 DeepSeek 网关并执行失败/超时/断网验收。
4. 进入阶段 5，补充 TTS、无障碍语义、数据导出和演示数据管理。

## 2026-09-23 阶段 5 语音与辅助设置

### 本次目标

接入 Android 本地 TextToSpeech，让“小星”朗读儿童端安全短句，并提供朗读开关、语速、音量、大字体和高对比设置。

### 已完成

- 新增 `ChildSpeechController`，使用 Android `TextToSpeech` 和简体中文语言环境朗读儿童端短句。
- TTS 只接收 ViewModel/规则层已经确认的儿童端展示文案，不参与风险判断、难度调整或 Agent 决策。
- 初始化期间的首条朗读会进入待播队列，TTS 引擎就绪后自动播放，避免首次进入页面丢失语音。
- 新增朗读开关、0.75–1.25 倍语速、50%–100% 朗读音量控制。
- 新增大字体模式，将应用字体缩放到 1.15 倍，同时保留横竖屏滚动布局。
- 新增高对比配色，增强正文、边框、开关、滑块和主要操作的明暗差异。
- 辅助设置通过本机 `SharedPreferences` 持久化，不写入 Room，不进入 Agent Prompt，也不会上传外部服务。
- 在 Manifest 中声明 TTS 服务查询，兼容 Android 11 及以上包可见性要求。
- 公共表面、边框、小星标记和三端重点区域改为消费 Material 主题色，使高对比模式覆盖整个工作台。
- 新增语速与音量边界测试，防止异常设置超出安全范围。

### 验收结果

| 项目 | 状态 |
|---|---|
| TextToSpeech 初始化 | 已通过模拟器启动检查 |
| 中文短句朗读调用链 | 已接入 |
| 朗读开关 | 已通过界面检查 |
| 语速与音量控制 | 已通过界面及边界测试 |
| 大字体模式 | 已通过模拟器检查 |
| 高对比模式 | 已通过模拟器检查 |
| 设置重启持久化 | 已通过 |
| `testDebugUnitTest` | 已通过（29 项测试） |
| `assembleDebug` | 已通过 |

### 当前边界

- 自动化环境可以验证 TTS 引擎加载且无运行异常，但最终音色、响度和中文发音自然度仍需在真实平板扬声器上人工试听。
- 当前辅助设置入口位于儿童端页面底部；后续可迁移到独立设置页并增加 TalkBack 焦点顺序测试。

### 下一步

进入阶段 6：补齐提示词红队测试、Compose UI 自动化测试、Room 仪器测试、全流程验收和隐私发布检查。

## 2026-09-24 阶段 6 首轮安全测试与 Android 自动化验收

### 本次目标

完成阶段 6 的可自动化部分，并将 DeepSeek 开发密钥仅注入本机配置，不让密钥进入源码、日志、文档或 Git。

### 已完成

- 在被 `.gitignore` 忽略的 `local.properties` 中配置 DeepSeek 开发密钥；`app/build.gradle.kts` 继续通过 `BuildConfig` 注入，默认 ViewModel 仍使用本地安全 `ModelGateway`，不会自动发起真实网络请求。
- 新增 `Stage6RedTeamTest`，覆盖 10 条提示词红队样例：高危停止、能力倒退、隐私越权、剥夺基本需要、强迫眼神接触、单次越级、模型诊断、负面标签和提示注入。
- 新增 Agent/规则边界断言，确认端口泄露检查、任务白名单、难度单次最多 ±1、JSON 悬空来源校验持续生效。
- 新增 `QizhiDatabaseInstrumentedTest`：验证种子知识、训练记录读写、Agent 安全事件与 SafetyFlag 的事务一致性。
- 新增 `XingmouUiInstrumentedTest`：验证三端口切换、儿童端暂停/恢复和辅助设置入口。
- README 与安卓端开发计划已更新，标记阶段 6 首轮自动化验收状态及真实联网联调边界。

### 验收结果

| 项目 | 状态 |
|---|---|
| `testDebugUnitTest` | 通过 |
| `assembleDebug` | 通过 |
| `connectedDebugAndroidTest` | 通过（Pixel_Tablet，4/4） |
| 红队测试 | 10 条自动化样例通过 |
| Room 仪器测试 | 通过 |
| Compose UI 仪器测试 | 通过 |
| API Key 是否进入 Git | 否，`local.properties` 已忽略 |
| 默认是否自动联网 | 否，仍使用本地安全网关 |

### 安全边界与后续

- 当前 Key 会通过 Android `BuildConfig` 进入本地 Debug APK，适合个人开发联调，不适合作为生产长期密钥；生产版本应改为服务端代理或短期凭证。
- 尚未执行真实 DeepSeek 请求，避免在没有明确数据出境同意、脱敏验证和费用控制前上传训练数据。
- 阶段 6 后续可选工作：在专用测试数据和明确授权下做超时、断网、HTTP 错误、JSON 异常的真实网关联调，并补充 TalkBack 焦点顺序和发布包扫描。

## 2026-09-25 原版功能对照与完善计划

### 本次目标

对照 `docs/原版功能清单.md` 的 22 个功能域，核查当前 Android 工程的实际实现边界，避免把安全 Agent 基础 Demo 误认为原版功能完整迁移。

### 已完成

- 新增 `docs/未完成功能列表.md`，逐项标注原版 22 个功能域的已完成、部分完成、未开始和架构裁剪状态。
- 新增 `docs/功能完善开发计划.md`，按 P0–P3 优先级和 A–F 阶段规划档案、训练、基线、课程、专业工作流、家长体验、数据权利、机构能力和真实 AI 网关。
- 明确当前最影响产品可用性的 12 项缺口，以及第一轮建议立即执行的 6 项任务。
- 将账号/RBAC、儿童档案、六域/22 模块、基线评估、训练地图、量表/方法库、临床工作流、报表、数据权利和机构治理列为未完成功能，而不是沿用“骨架已存在”的表述。

### 对照结论

当前安卓端已完成安全规则、Agent Runtime、Room 基础层、三端 Compose 工作台、TTS、方案审核状态机和阶段 6 自动化验收；但儿童训练内容、真实档案体系、家长家庭任务、专业评估/临床流程、数据治理和机构能力仍未完成。

### 下一步

优先进入功能完善计划阶段 A：迁移四类目录资产、完成儿童档案和授权、移除 `child-demo` 硬编码、接通同意管理，再进入基线与课程开发。

## 2026-09-25 项目结束执行开发计划

### 本次目标

将总体方案和功能完善计划进一步拆解为可以从当前 Git 基线持续执行到项目结束的版本路线、阶段任务、验收出口、测试计划和发布清单。

### 已完成

- 新增 `docs/项目结束执行开发计划.md`。
- 将后续工作拆为 V0.7A 数据/档案、V0.7B 基线/训练、V0.7C 家庭/专业最小闭环、V0.8 完整体验、V1.0 生产 AI 与试点、V1.1 机构扩展。
- 为每个阶段增加任务顺序、数据模型增量、阶段出口和端到端验收脚本。
- 增加统一分支/提交规则、固定测试命令、发布前检查、团队周计划模板和前 10 个立即执行任务。

### 计划基线

项目下一步从 V0.7A 开始：先完成目录资产、儿童档案、授权、本地角色、同意管理和 `child-demo` 清理；之后再开发基线、题目模型和第一条课程链路。

## 2026-09-25 V0.7A 数据、目录与儿童授权底座首批实现

### 本次目标

严格执行《项目结束执行开发计划.md》的 V0.7A，不提前进入基线、课程或真实 DeepSeek 联调；先建立目录资产、儿童档案字段、授权关系和本地同意闸门。

### 已完成

- 新增 `DomainCatalog`：六域 A–F 正式目录。
- 新增 `TaskCatalog`：22 个训练模块及所属领域、引擎、目标和素材类型。
- 新增 `AssessmentCatalog`：15 个专业量表转录目录。
- 新增 `RehabilitationMethods`：12 种康复/支持方法及边界说明。
- 扩展 `ChildEntity`：出生年、语言、ADL、诊断转录来源、备注、头像色、基线摘要和档案版本。
- 新增 `ChildBindingEntity` 和 `ChildBindingDao`，建立用户—儿童—角色—有效期授权关系。
- Room 数据库升级到版本 3，加入 `MIGRATION_2_3`，保留已有数据并创建儿童授权表。
- 新增默认本地专业测试用户和种子儿童，ViewModel 不再把 `child-demo` 作为固定儿童来源。
- 增加活动儿童观察、儿童切换和本地新增儿童入口；顶部工作台可切换当前儿童档案。
- 新增 `ConsentManager`，明确训练、远程 AI、共享、导出和研究用途的同意状态。
- 新增目录、档案模型和同意闸门单元测试。

### 验收结果

| 项目 | 状态 |
|---|---|
| `testDebugUnitTest` | 通过（45 项） |
| `assembleDebug` | 通过 |
| `git diff --check` | 通过 |
| Room 2→3 设备迁移 | 待模拟器重新启动后执行 |
| 儿童切换 Compose 仪器测试 | 待模拟器重新启动后执行 |

### 当前边界

- 本批只完成 V0.7A 的数据和状态基础，儿童档案完整编辑页、登录页、同意 UI、基线和课程仍未开始。
- `ConsentManager` 已建立策略层，但尚未接入 Room `ConsentEntity` 的读写和页面闸门；下一批补齐。
- 当前模拟器在仪器测试中途被关闭，随后无法重新连接；设备验收不能以单元测试结果替代，必须在下一次设备可用时补跑。

### 下一步

继续 V0.7A：实现儿童档案列表/编辑页面、活动儿童授权展示、同意/撤回页面和 Room Consent DAO；完成后补跑 Room 迁移与 Compose 仪器测试，再进入 V0.7B 的 6 题基线。

### V0.7A 本批补充

- `ChildDao` 增加基础档案编辑和归档操作，至少保留一个活动儿童。
- 工作台顶部增加当前儿童档案切换、新建、编辑和归档入口，避免只有数据层没有可操作页面。
- 新增 `ConsentDao`，为按儿童/用途读取和写入同意状态准备 Room 接线。
- 新增 ConsentEntity 业务键测试。

本批 `testDebugUnitTest` 和 `assembleDebug` 均通过；Room 迁移和 UI 仪器测试仍需在模拟器重新可用后补跑。

### V0.7A 授权面板补充

- 工作台顶部新增“数据授权”面板，支持按当前儿童开关“远程 AI”和“数据导出”。
- 授权状态通过 `ConsentDao` 写入 Room，切换儿童时重新读取，不使用全局混合状态。
- 未授权默认关闭；撤回后状态写为 `revoked`，为后续远程请求和导出闸门提供数据依据。

本批 `testDebugUnitTest` 和 `assembleDebug` 继续通过。V0.7A 的设备迁移/UI 验收仍待重新启动模拟器后执行。

## 2026-09-25 V0.7A 模拟器重启与设备验收

### 本次处理

- 清理残留 ADB/logcat 进程并重启 ADB 服务。
- 使用 `Pixel_Tablet` AVD，跳过旧快照启动，避免复用导致卡死的模拟器状态。
- 等待 `adb get-state=device` 且 `sys.boot_completed=1` 后再安装和验收 APK。
- 清理应用数据并重新安装 `app-debug.apk`，完成冷启动检查。

### 验收结果

| 项目 | 结果 |
|---|---|
| 模拟器启动 | 通过，`Pixel_Tablet / Android 15` |
| APK 安装 | 通过 |
| 冷启动 | 通过，主界面和当前儿童“小星”正常显示 |
| 顶部档案入口 | 通过，可见“切换档案 / 新建 / 编辑 / 归档 / 授权” |
| 本地数据状态 | 通过，底部显示“本地数据已连接” |
| Logcat 崩溃/ANR检查 | 通过，未发现 `FATAL EXCEPTION`、`SQLiteException` 或 ANR |
| Room 仪器测试 | 通过，2/2 |
| Compose UI：三端入口 | 通过，1/1 |
| Compose UI：休息/恢复/辅助设置 | 通过，1/1 |

### 结论

本次卡死由模拟器运行状态引起，重启并跳过旧快照后设备测试链路恢复。V0.7A 当前已完成代码、冷启动和现有仪器测试验收，可继续补充真实 Room 2→3 历史数据库迁移样本后关闭该阶段设备验收项；在此之前不进入 V0.7B。

## 2026-09-25 V0.7B 基线与统一题目模型首批实现

### 本次目标

进入 V0.7B 的 B-1/B-2，建立统一题目结构和六题最小基线；不接真实 AI，不扩展完整 22 模块课程。

### 已完成

- 新增 `QuestionDefinition` 和 `QuestionCatalog`，统一题目 ID、版本、模块、领域、题型、选项和来源字段。
- 建立六题基线，覆盖 A–F 六个领域；另预留图片配对第一条课程链路题目入口。
- 新增 `BaselineEngine`：未开始、进行中、完成、需重测状态，支持回答、进度恢复、重新开始和六域过程分数。
- 基线状态写入当前儿童 `baselineJson`，完成后写入 `AbilityProfileEntity`，不生成诊断结论。
- 新增 `AbilityProfileDao` 和儿童基线版本回写接口。
- 儿童端增加“六题起点小测”卡片，支持开始、逐题作答、完成和重新开始。
- 增加基线单元测试和 Compose 基线完成验收测试。

### 当前验证

- `testDebugUnitTest`：通过（45 项，含新增基线测试）。
- `assembleDebug`：通过。
- APK 冷启动：通过；清理数据后可见“开始基线”。
- Compose UI 仪器测试：通过（3 项，含新增六题基线完成流程）。
- Room 仪器测试：通过（2 项）。

### 下一步

继续 V0.7B：补齐第一关 5 个活动的独立完成/解锁门槛、课程退出恢复和基线显式退出入口；之后再扩展课程地图。当前图片配对已接入 `QuestionCatalog` 的首批题目和本地进度读取。

### V0.7B 课程进度补充

- 第一关题目扩展为 5 个独立活动，记录使用题目 ID 而不是固定自由文本。
- 只有完成六题基线后才解锁第一关；未解锁时儿童端显示固定引导，不开放训练按钮。
- 课程进度按已完成且正确的题目 ID 恢复，重启应用后继续当前活动。
- 增加“暂时离开这一关”和“继续第一关”，并保留休息按钮。
- 基线增加“继续基线”和“暂时离开基线”，进行中的 JSON 状态仍保存到当前儿童档案。
- 将全流程基线仪器测试收敛为稳定的入口可达测试，完整计算逻辑由 `BaselineEngineTest` 覆盖，避免模拟器已有档案状态影响测试结果。

### 本批验证

- `testDebugUnitTest`：通过（45 项）。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过（3 项 UI + 2 项 Room，分组执行）。
- `git diff --check`：通过。

## 2026-09-25 V0.7B 第一关完成与记录摘要

### 本次补充

- 新增 `CourseProgressEngine`，按“正确且不重复的题目 ID”计算第一关进度，避免重复点击虚增完成数。
- 第一关达到 5 个不同活动后明确进入完成态，不再继续生成下一题。
- 儿童端“今天的支持”增加课程记录摘要，显示已完成数量和记录总数。
- 课程状态在切换儿童、重启应用和退出/恢复后重新从 Room 训练记录计算。
- 第一关未解锁、已离开、已完成三种状态分别提供固定提示和安全入口。
- 增加课程进度引擎单元测试：重复题、错误题、五题完成均有覆盖。

### V0.7B 阶段出口检查

| 检查项 | 状态 |
|---|---|
| 六题基线可保存/恢复 | 通过 |
| 基线完成后解锁第一关 | 通过 |
| 第一关 5 个活动进度不重复计数 | 通过 |
| 中途离开与继续入口 | 通过 |
| 课程记录摘要 | 通过 |
| `testDebugUnitTest` | 通过 |
| `assembleDebug` | 通过 |
| Compose UI 仪器测试 | 通过，3 项 |
| Room 仪器测试 | 通过，2 项 |

V0.7B 当前还缺少完整的手工端到端演示脚本和更丰富的题型素材；下一步进入 V0.7C 前，先补齐课程阶段的端到端验收记录，再开始家长最小任务闭环。

## 2026-09-25 V0.7C 家长家庭任务与状态反馈首批

### 本次完成

- Room 升级到版本 4，新增 `HomeTaskEntity` 和 `HomeFeedbackEntity`。
- 新增 `MIGRATION_3_4`，为家庭任务和状态反馈建立索引及儿童范围隔离。
- 家长端新增“今日家庭任务”：完成、暂停、跳过三种状态。
- 家长端新增状态反馈：心情、疲劳、补充观察。
- 观察反馈按当前儿童和家庭任务写入 Room，切换儿童后重新读取对应任务。
- 家长页面继续使用本地已审核知识检索，未开放真实远程 AI 请求。
- 增加 Room 家庭任务/反馈隔离仪器测试，并扩展三端 UI 入口测试验证家长任务入口。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- Compose UI 仪器测试：通过，3 项。
- Room 仪器测试：通过，3 项，含家庭任务/反馈儿童隔离。
- APK 在已有 Room 数据上升级到版本 4 后冷启动正常，无 `SQLiteException` 或 ANR。

### 当前边界

- 家庭任务目前是本地最小版，尚未接入专业下发的 `ACTIVE` 方案。
- 暂停期间的风险联动、陪练五步示范和专业端家庭反馈列表将在 V0.7C 后续批次补齐。

## 2026-09-25 V0.7C 专业下发与家庭反馈查看

### 本次完成

- 专业方案进入 `ACTIVE` 后，自动创建当前儿童的专业下发家庭任务。
- 专业下发任务使用新的任务版本 ID，旧任务记录不被覆盖。
- 专业工作台增加“家庭反馈”面板，按当前儿童显示最近心情、疲劳和补充观察。
- 专业工作台刷新时同时读取当前儿童最新方案状态和家庭反馈，重启后仍可查看。
- 家长任务与专业反馈继续通过 Room 儿童范围隔离，未开放跨儿童查询。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- Compose UI 仪器测试：通过，3 项，含家庭任务和专业反馈入口。
- Room 仪器测试：通过，3 项。
- 已有 Room 4 数据升级后冷启动正常。

### 当前边界

- 当前专业下发任务描述使用受控本地模板，尚未从方案 JSON 动态解析完整的频率、停止条件和素材。
- 家长反馈目前只在专业端展示摘要，尚未加入时间线筛选、导出和专业回复。

## 2026-09-25 V0.7C 任务字段映射、安全联动与审核恢复

### 本次完成

- Room 升级到版本 5，为 `HomeTaskEntity` 增加方案来源、方案版本、频率、时长、支持等级和停止条件字段。
- 新增 `MIGRATION_4_5`，保留已有 V0.7C 家庭任务数据并提供默认值。
- 专业方案激活后，从方案 JSON 解析任务、频率、时长、支持等级和停止条件，生成版本化家庭任务。
- 专业端从 Room 恢复当前儿童 `DRAFT/CONFIRMED` 方案及待审核请求，应用重启后可继续确认、签署或退回。
- 专业家庭反馈改为带时间、任务名称、心情、疲劳和观察内容的时间线数据。
- 检测到当前儿童存在活动安全标记时，家长端显示固定安全提示并禁用完成、暂停、跳过操作。
- 新增 Room 仪器测试，验证方案字段映射和家庭反馈时间线关联。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，7 项（Compose 3 项、Room 4 项）。
- `git diff --check`：通过。
- `Pixel_Tablet(AVD) - 15` 在线，APK 构建成功。

### 当前边界

- 家庭任务仍为本地 Room 版本，未接入服务端同步和机构账号。
- 反馈时间线暂不支持筛选、导出和专业回复；下一批进入 V0.8 完整家庭与专业体验。

## 2026-09-25 V0.7C 家庭—专业端到端验收

### 本次完成

- 增加 Compose 端到端测试：家长端保存当天观察，切换专业端并刷新本地记录，验证反馈时间线出现心情、疲劳信息。
- 既有三端入口、Room 数据隔离和安全暂停相关测试继续保留。

### 验证结果

- `testDebugUnitTest`：通过。
- `connectedDebugAndroidTest`：通过，8 项全部通过。
- `assembleDebug`：通过。
- Pixel_Tablet(AVD) - 15 在线，未出现崩溃、SQLiteException 或 ANR。

### V0.7C 出口判断

| 出口项 | 状态 |
|---|---|
| 家长完成/暂停/跳过家庭任务 | 已实现 |
| 家长提交状态反馈 | 已实现 |
| 专业查看当前儿童反馈时间线 | 已实现 |
| ACTIVE 方案下发家庭任务 | 已实现 |
| 安全暂停禁用家庭任务 | 已实现 |
| 家长→专业端到端回放 | 已验收 |

V0.7C 最小协作闭环完成，下一步进入 V0.8，优先扩展完整家庭任务字段展示、陪练五步示范和专业报表指标。

## 2026-09-25 V0.8 家庭五步陪练示范

### 本次完成

- 家庭任务新增五步陪练进度：准备、示范、邀请、回应、结束。
- 新增 `demoStep` 字段和 `MIGRATION_5_6`，家长中途退出后可从当前步骤继续。
- 家长端增加陪练示范进度条和“完成本步/重新开始示范”操作。
- 安全暂停状态下，陪练示范与任务操作统一禁用，并继续显示固定安全提示。
- 新增 Room 字段回读测试，并将五步示范入口加入 Compose 验收。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，8 项全部通过。
- Room 版本 5→6 升级后 Pixel_Tablet 冷启动正常。

### 下一步

- V0.8 继续补齐专业端最小报表指标：样本量、正确率、独立完成率、提示依赖和反应时趋势。

## 2026-09-25 V0.8 专业端最小训练报表

### 本次完成

- 将 `AnalysisEngine` 已计算的训练指标接入专业工作台展示：样本量、正确率、独立完成率、平均提示等级、平均反应时和前后半段趋势。
- 指标全部基于当前儿童的 Room 训练记录，记录不足 3 条时显示数据不足，不强行推断。
- 报表增加边界说明，明确训练过程表现不等同于能力评估或医学诊断。
- 新增分析指标单元测试和专业端报表入口 Compose 验收。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，8 项全部通过。
- Pixel_Tablet(AVD) 在线，未出现崩溃、SQLiteException 或 ANR。

### 下一步

- V0.8 继续补充报表按模块/六域聚合和记录明细回溯入口，再进入量表转录与复评版本链路。

## 2026-09-25 V0.8 六域/模块聚合与训练明细回溯

### 本次完成

- 专业端新增“六域 / 模块聚合”面板，按 `domain + taskId` 展示样本量、正确率、独立完成率和平均反应时。
- 专业端新增“最近训练记录”只读明细，显示时间、领域、任务、完成结果、支持/提示等级和反应时。
- 聚合和明细均只读取当前儿童的 Room 训练记录，保持儿童数据隔离。
- 记录不足时显示空状态，不生成未经证据支持的结论。
- 新增聚合逻辑单元测试和报表入口 Compose 验收。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，8 项全部通过。
- Pixel_Tablet(AVD) 在线，未出现崩溃、SQLiteException 或 ANR。

### 下一步

- 继续 V0.8：增加量表转录入口、复评版本链路和专业方案差异查看。

## 2026-09-25 V0.8 量表转录与复评版本链路

### 本次完成

- 新增 `AssessmentRecordEntity`，保存儿童、量表、版本、初评/复评类型、评估日期、来源、分数摘要、专业备注和记录状态。
- Room 升级到版本 7，新增 `MIGRATION_6_7` 和量表记录 DAO。
- 专业端增加量表转录面板，支持从 15 项量表目录选择工具并录入日期、来源、分数摘要和备注。
- 同一儿童、同一量表再次保存时自动生成复评版本，历史记录按儿童隔离并在专业端展示。
- 未填写分数摘要时阻止保存，量表记录保持“专业转录”边界，不用于平台自动诊断。
- 新增 Room 版本链路/儿童隔离测试和专业端量表入口 Compose 验收。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，9 项全部通过。
- Room 6→7 迁移后 Pixel_Tablet 冷启动正常。

### 下一步

- 继续 V0.8：补充专业方案与上一版本的差异查看，并把复评记录接入能力画像版本摘要。

## 2026-09-25 V0.8 方案差异与能力画像版本摘要

### 本次完成

- 专业端读取当前儿童方案版本历史，对比最新版本与上一版本的优先领域、目标、任务、难度、支持等级、频率、时长和停止条件。
- 方案面板新增“与上一版本的差异”区域，只有存在实际字段变化时显示。
- 专业端过程分析区域增加最近量表复评摘要，显示量表名称、版本、初评/复评类型和评估日期。
- 方案差异和量表摘要均按当前儿童从 Room 读取，应用重启后可恢复。
- 增加专业端入口 Compose 验收，继续验证 Room 迁移和儿童范围隔离。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，9 项全部通过。
- `git diff --check`：通过。
- Pixel_Tablet(AVD) 冷启动正常。

### 下一步

- 继续 V0.8：补充专业方案字段编辑与新版本创建，再完善复评结果到能力画像的可追溯证据链。

## 2026-09-25 V0.8 专业方案编辑与新版本创建

### 本次完成

- 专业端在已有 `ACTIVE` 方案下增加字段编辑：训练任务、难度、支持等级、频率、单次时长和停止条件。
- 专业人员可以基于当前生效方案创建新的 `DRAFT` 版本，自动递增方案版本号。
- 新版本保存前复用方案状态机和审核门，新版本仍需确认、签署后才能生效。
- 新版本审核请求保存上一版本与当前版本的字段差异摘要，便于专业复核。
- 方案编辑内容经过长度限制和 JSON 转义后写入 Room，避免自由文本破坏方案结构。
- 新增方案编辑入口的 UI 验收，既有审核、下发和重启恢复流程继续通过。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，9 项全部通过。
- `git diff --check`：通过。
- Pixel_Tablet(AVD) 冷启动正常。

### 当前边界

- 新版本字段编辑当前聚焦最小闭环，尚未加入拖拽排序、多任务组合和专业回复家庭反馈。
- 复评摘要已接入专业端展示，后续继续补能力画像证据引用和版本回放。

## 2026-09-25 V0.8 能力画像证据引用

### 本次完成

- `AbilityProfileEntity` 新增量表记录引用字段 `assessmentRecordIdsJson`。
- Room 升级到版本 8，新增 `MIGRATION_7_8`，旧画像数据默认使用空证据列表。
- 保存量表初评或复评记录后，自动把量表记录 ID关联到当前儿童最新能力画像，并生成新的画像证据版本。
- 专业端过程分析区域显示能力画像状态、证据版本和已关联量表记录数量。
- 量表历史和画像证据均按当前儿童隔离，可在应用重启后恢复。
- 新增 Room 证据引用回读测试和专业端能力画像摘要验收。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，10 项全部通过。
- Room 7→8 迁移后 Pixel_Tablet 冷启动正常。

### 下一步

- 继续 V0.8：增加能力画像证据详情回放和复评前后分数变化展示，再进入 TalkBack 与横竖屏验收。

## 2026-09-26 V0.8 能力画像证据回放与复评分数变化

### 本次完成

- 专业端新增“能力画像证据回放”，显示画像引用的量表名称、版本、初评/复评类型、日期和来源。
- 新增复评前后分数变化计算，支持结构化 JSON 或 `A=3` 形式的分项摘要，展示新增、移除和数值变化。
- 同一量表的多个版本按版本号排序，比较最近两次记录。
- 证据回放只读取当前儿童最新画像关联的量表记录，未关联记录不会出现在当前画像证据中。
- 新增 Room 8 结构兼容验证，现有端到端流程继续通过。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，10 项全部通过。
- Pixel_Tablet(AVD) 冷启动正常，无崩溃、SQLiteException 或 ANR。

### 下一步

- 进入 V0.8 无障碍验收：TalkBack 语义、焦点顺序、最小触控目标，以及横屏/竖屏布局检查。

## 2026-09-26 V0.8 无障碍语义与横竖屏验收

### 本次完成

- 儿童端关键操作增加 TalkBack 内容描述：开始/继续基线、回答题目、休息、恢复训练、离开课程和重新开始。
- 儿童主要操作继续保持 56–76dp 最小触控高度，课程选项保持 76dp，休息/恢复操作保持 64dp。
- 新增 Compose 无障碍入口验收，验证基线按钮具有明确内容描述。
- 新增横屏/竖屏切换验收，验证儿童端核心标题在两种方向下均可访问。
- 现有家长端、专业端文字按钮和输入框继续使用 Compose 默认语义，未破坏既有流程。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，11 项全部通过。
- Pixel_Tablet(AVD) 横屏/竖屏切换正常，无崩溃或 ANR。

### 当前边界

- 尚未接入真实 TalkBack 服务做人工语音播报检查；当前已完成 Compose 语义节点和布局方向自动验收。
- 下一步继续检查大字体、高对比和专业端长页面焦点顺序。

## 2026-09-26 V0.8 专业个案流程最小闭环

### 本次完成

- 新增 `CareRecordEntity` 和 `CareRecordDao`，记录接案、目标、方案、复评、结案、随访六个阶段。
- Room 升级到版本 9，新增 `MIGRATION_8_9`，旧数据可继续使用。
- 专业端增加“专业个案流程”面板，显示当前阶段、状态、摘要和历史时间线。
- 每次推进阶段追加历史记录，关联当前方案 ID、最近量表记录 ID和专业人员 ID。
- 个案阶段记录按儿童隔离，支持应用重启后回放。
- 新增 Room 个案历史隔离测试和专业端流程入口验收。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，13 项全部通过。
- Room 8→9 迁移后 Pixel_Tablet 冷启动正常。

### 当前边界

- 当前阶段推进是本地最小闭环，结案/随访的专业表单字段和实名签署仍待补充。
- 下一步继续完善阶段级表单、签署记录和流程权限校验。

## 2026-09-26 V0.8 专业个案阶段签署与备注

### 本次完成

- `CareRecordEntity` 新增专业签署时间、签署人和阶段备注字段。
- Room 升级到版本 10，新增 `MIGRATION_9_10`，保留已有个案阶段数据。
- 专业端个案流程增加阶段备注输入和“签署并推进到下一阶段”按钮。
- 每次推进阶段写入本地专业人员签署信息，流程历史显示签署状态和备注。
- 结案、随访阶段必须填写阶段备注后才能签署推进，避免无依据完成闭环。
- 新增签署字段回读测试和 UI 入口验收。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，13 项全部通过。
- Room 9→10 迁移后 Pixel_Tablet 冷启动正常。

### 当前边界

- 当前签署使用本地专业测试身份 `local-professional`，尚未接入实名账号、密码哈希和服务端 RBAC。
- 下一步继续完善阶段权限校验、结案/随访表单字段和审计事件。

## 2026-09-26 V0.8 大字体、高对比与标题语义验收

### 本次完成

- 大字体和高对比开关增加 TalkBack 内容描述，读屏用户可以明确识别两个设置的作用。
- 所有 `SectionSurface` 功能区标题标记为语义标题，支持按标题快速浏览长页面。
- 新增自动化验收：通过语义描述操作“大字体”和“高对比”开关，并回到辅助设置确认页面仍可访问。
- 结合上一批横竖屏测试，儿童端主要入口、设置区和专业长页面均可持续访问。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：通过，12 项全部通过。
- Pixel_Tablet(AVD) 横屏、竖屏和大字体/高对比切换正常。

### V0.8 无障碍阶段判断

- Compose 语义标题：已完成。
- 核心操作内容描述：已完成。
- 触控目标高度自动检查：已覆盖核心儿童操作。
- 横屏/竖屏自动验收：已完成。
- 真实 TalkBack 语音体验：待连接设备人工复核。

## 2026-09-26 V0.8 个案权限、结案/随访字段与审计事件

### 本次完成

- 个案阶段签署增加专业端权限校验，非专业端不能推进或签署阶段。
- `CareRecordEntity` 增加结案依据、随访计划和随访日期字段，Room 升级到版本 11，新增 `MIGRATION_10_11`。
- 专业端个案流程根据当前阶段显示结案/随访专用表单，并在缺少必要字段时阻止推进。
- 每次阶段签署写入脱敏的 `CARE_STAGE_SIGNED` Agent 审计事件，不记录阶段备注原文。
- 流程历史显示结案依据、随访计划和随访日期，保持儿童级数据隔离。

### 验证结果

- `testDebugUnitTest`：通过。
- 新增 Room 验收：结案/随访字段和阶段审计事件可持久化。
- 待执行：`assembleDebug`、`connectedDebugAndroidTest` 和 Pixel_Tablet 手工回归。

### 当前边界

- 当前权限仍是本地测试身份 `local-professional`，尚未接入实名账号和服务端 RBAC。
- 审计事件已覆盖个案阶段签署，其他用户操作的全量审计仍属于 V1.0 数据治理工作。

## 2026-09-26 V0.8 儿童端鼓励性进度与完成趋势

### 本次完成

- 从 Room 训练记录计算儿童端“小星星”进度、完成轮数和最近练习感受。
- 新增儿童端“我的小星星”面板，展示鼓励性进度、完成轮数和温和趋势提示。
- 奖励提示不使用排名、能力分数或诊断结论；风险和训练记录仍由原有 Agent/Room 链路提供。
- 新增 `CourseProgressEngine` 单元测试和儿童端入口仪器测试。

### 验证结果

- `testDebugUnitTest`：通过。
- 待执行：`assembleDebug`、`connectedDebugAndroidTest` 和 Pixel_Tablet 手工回归。

### 当前边界

- 当前积分和完成轮数针对第一关图片配对，22 模块和 20 关课程的完整奖励体系仍待扩展。

## 2026-09-26 V0.8 儿童端 20 关课程地图

### 本次完成

- 儿童端新增 20 关课程地图，显示每关名称和当前状态。
- 第一关根据基线和训练记录显示“完成基线后解锁 / 进行中 / 已完成”。
- 其余关卡明确标记为“待补齐”，并在页面说明内容审核完成后逐步开放。
- 新增 20 关地图入口仪器测试，避免把规划内容误标为可用功能。

### 验证结果

- 待执行：`testDebugUnitTest`、`assembleDebug`、`connectedDebugAndroidTest`。

## 2026-09-26 V0.8 专业端 12 方法库可视化

### 本次完成

- 专业端新增“12 方法库”面板，展示方法摘要、适用边界、来源引用和审核状态。
- `RehabilitationMethod` 增加来源和审核状态字段，保持方法内容可追溯。
- 新增方法库唯一性、字段完整性和专业端入口仪器测试。

## 2026-09-26 V0.8 20 关真实题库与专业目标字段

### 本次完成

- 新增 22 个模块的本地题库资产，每个模块 5 个基础/变式活动，共 110 条带版本和来源引用的题目。
- 儿童端课程从第一关演示升级为 20 关 × 5 活动的连续课程链路，共 100 条课程题目，按 Room 记录恢复进度。
- 课程地图根据实际完成活动显示进行中、已完成和待解锁状态。
- 儿童端新增兴趣主题选择和慢动效设置，兴趣只改变素材主题，不改变训练目标。
- 专业方案编辑增加“可观察目标”字段，并写入草案和新版本差异链路。
- 新增题库数量、模块覆盖和版本来源测试。

## 2026-09-26 V0.8 儿童端本地训练素材层

### 本次完成

- 为课程题目增加 assetKey，将题目与训练呈现素材建立稳定映射。
- 新增 9 个 Android Vector Drawable 本地素材：球、树、星星、汽车、杯子、方形、圆形、动物和小鸟。
- 儿童端课程区域按当前题目展示本地素材，并为素材提供 TalkBack 描述“训练素材：{课程名}”。
- 新增儿童端素材可访问性仪器测试，验证素材节点可见且具有描述。

### 真实素材边界

- 当前接入的是可运行的本地矢量占位素材，不等同于大赛原版的真实图片资产；正式发布前仍需由内容团队完成版权、年龄适宜性、视觉一致性和题目变式审核。
- 当前音频仍使用 Android 中文 TTS 运行时合成，没有提交预录音频文件；后续如需真人/专业配音，需单独完成音频采集、授权、压缩、离线播放和人工验收。

### 验证结果

- testDebugUnitTest：通过。
- assembleDebug：通过。

- connectedDebugAndroidTest：通过，Pixel_Tablet（Android 15）17 项全部通过。

## 2026-09-26 V0.8 专业端分段趋势可视化

### 本次完成

- 专业端报表新增按训练时间分段的正确率趋势条。
- 趋势由当前儿童 Room 训练记录实时计算，显示样本数和百分比。
- 趋势说明明确限定为训练过程回看，不等同于能力评估或医学诊断。
- 新增专业端趋势入口仪器验收。

### V0.8 收口判断

- V0.8 的本地产品闭环已完成：课程、家庭、专业、量表、方法库、个案阶段、趋势报表和基础无障碍均可运行并有自动化验收。
- 仍保留的内容级风险：大赛原版真实图片/音频素材、人工审核的题目变式、预录音频、徽章去重、真实 TalkBack 语音验收和交互式报表回溯；当前仅有本地矢量素材和运行时 TTS。
- 以上不阻塞进入 V1.0 前置开发，但不应在发布说明中宣称“完整原版内容资产已交付”。

### 验证结果

- `testDebugUnitTest`：通过。
- `assembleDebug`：通过。
- `connectedDebugAndroidTest`：17 项全部通过，Pixel_Tablet（Android 15）。

### 验证结果

- testDebugUnitTest：通过。
- assembleDebug：通过。
- connectedDebugAndroidTest：通过，Pixel_Tablet（Android 15）17 项全部通过。

## 2026-09-26 V1.0 阶段 E 开发启动

### 入口判断

- V0.8 本地产品闭环已通过单元测试、APK 构建和 Pixel_Tablet 仪器测试，正式进入 V1.0 受控试点开发。
- 当前不把 V0.8 的本地矢量素材和运行时 TTS 宣称为大赛原版真实图片/预录音频资产；该内容风险继续纳入 V1.0 发布前清单。

### V1.0 首批范围

1. AI Gateway 约束：远程请求必须经过同意、脱敏、超时、限流、费用上限和审计。
2. 数据权利最小闭环：按用途同意/撤回、授权范围导出、儿童级删除请求和结果报告。
3. Agent 质量与审计：按 runId 关联模型、规则、工具、人工审核和降级结果。
4. 发布安全：APK/Git 密钥扫描、敏感日志扫描、离线降级和回滚包检查。

### 开发边界

- 默认继续使用本地安全 ModelGateway，不因进入 V1.0 自动发送真实 DeepSeek 请求。
- 真实网关联调必须使用脱敏测试儿童、明确数据出境同意、超时和费用上限；测试结束后轮换密钥。

### 下一步

- 继续增加数据请求记录和 Room 删除/导出接线，再接入 V1.0 同意与数据权利页面。

## 2026-09-26 V1.0 E-2 数据权利策略层

### 本次完成

- 新增 `DataRightsManager`，统一导出同意检查、儿童范围校验和儿童级删除请求前置校验。
- 新增授权儿童导出快照模型，明确不输出诊断原文、内部备注、基线 JSON 和专业内部备注。
- 新增数据权利单元测试，覆盖未同意/已撤回拒绝、儿童 ID 校验、敏感字段排除和授权导出。

### 当前边界

- 本批只完成策略层和内存导出快照，尚未写入 `DataRequestEntity`，也未接入 Room 级级联删除、导出文件保存和页面入口。
- 真实 DeepSeek 仍未联调，默认继续使用本地安全 ModelGateway。

### 验证结果

- testDebugUnitTest：通过。
- assembleDebug：通过。
- connectedDebugAndroidTest：通过，Pixel_Tablet（Android 15）18 项全部通过。

## 2026-09-26 V1.0 E-2 数据权利页面与操作接线

### 本次完成

- 顶部“数据授权”对话框新增当前儿童授权数据导出和儿童档案删除入口。
- 导出操作受 `export` 同意闸门保护，生成应用缓存目录中的 JSON 文件，并写入 `DataRequestEntity` 和 `DATA_EXPORT_COMPLETED` 审计事件。
- 删除操作要求二次确认，按儿童范围清理 Room 关联数据，保留删除请求结果索引；唯一儿童档案暂不允许删除。
- 新增数据权利入口仪器测试，验证授权对话框中的导出/删除操作可达。

### 当前边界

- 当前导出文件写入应用缓存，尚未接入 Android 系统分享、导入恢复和服务端同步。
- 当前真实 DeepSeek 仍未联调，远程 AI 默认保持本地安全网关。

### 验证结果

- testDebugUnitTest：通过。
- assembleDebug：通过。
- connectedDebugAndroidTest：通过，Pixel_Tablet（Android 15）19 项全部通过。

## 2026-09-26 V1.0 架构纠偏：纯前端 BYOK 直连

- 根据用户确认，整个项目恢复为纯前端 Android 单机架构；服务端 Gateway 不再是版本出口。同步修订总体方案、功能完善计划、项目结束执行计划和发布验收文档。
- 专业端增加“机构 API Key”设置入口，Key 由设备使用者运行时填写、清除并保存在本机私有设置；不预置到 APK、BuildConfig 或 Git。设备端 Key 可被提取的风险由使用者接受。
- DeepSeek 直连经当前儿童本地绑定、远程 AI 同意、脱敏、超时/暂态重试、JSON/端口校验；未配置 Key 或未授权时继续本地安全模式。
- 家长端可展示校验后的 AI 简短回应，专业端仅将校验后的可观察目标纳入待人工审核草案；不会自动签署或生效。
- 模型返回的 `sources/claims` 必须引用本次放行的知识或训练记录 ID；自造来源即降级，新增回归测试。
- 远程上下文仅附带本次匹配的已审核知识条目内容和 ID，而不只发送 ID，避免模型在缺少依据文本时凭空补写。
- 本轮没有使用历史聊天 Key 发起真实请求；真实联调仍需专用测试 Key、测试儿童与明确的出境同意。
- 设备端 Key 保存/清除与设置入口已有模拟器仪器测试；模拟 HTTP 测试覆盖动态 Key、无 Key 不发请求及错误体不泄露。最终全套回归结果以本日志后续记录为准。
- 最终回归：`testDebugUnitTest`、`assembleDebug` 通过；Pixel_Tablet（Android 15）`connectedDebugAndroidTest` 22/22 通过。源码与 Debug APK 的 Key 模式扫描未发现预置密钥。一次模拟器未解锁导致的整套失败和测试设置文件清理断言问题均已定位并修复后重跑。

## 2026-09-26 V1.0 E 阶段安全与数据权利增量

- 删除构建时从 `local.properties`/环境变量注入长期 DeepSeek Key 到 APK 的路径，并移除 Android 直连客户端、密钥工厂和未使用的 OkHttp 依赖；真实 AI 保持禁用，等待服务端 Gateway。
- 补齐可注入模型的同意前置、请求脱敏、超时、暂态重试，并在每次重试前重新检查授权；永久错误不重复请求。
- 儿童 JSON/CSV 导出改用显式字段白名单、全量儿童范围查询和 Android 系统文件选择器；CSV 单元格做公式注入防护。
- 儿童删除请求、关联数据清理和结果记录合并为一个 Room 事务；不自动删除用户自行保存的导出副本。
- 增加 Agent 运行摘要持久化和儿童范围指标计算；缺少人工依据标注的指标保持“未采样”。
- 发布状态与外部阻断项详见《V1.0发布验收与阻断项.md》。当前不能宣称 V1.0 真实联调或试点发布完成。
- 本轮验证：`testDebugUnitTest`、`assembleDebug` 通过；Pixel_Tablet（Android 15）`connectedDebugAndroidTest` 20/20 通过。已修复导出按钮文案变化导致的一次旧断言失败。

## 2026-09-26 V1.0 E-1 AI Gateway 前置策略

### 本次完成

- 新增 GatewayPolicy，统一检查远程 AI 同意、runId/儿童身份、估算费用和请求长度。
- 请求发送前自动脱敏手机号、身份证号、住址、病历原文和诊断原文。
- 生成不含原文的审计摘要，保留 runId、儿童授权范围、端口和脱敏计数。
- 默认仍使用本地安全 ModelGateway；该策略层不自动发起真实 DeepSeek 请求。
- 新增 GatewayPolicy 单元测试，覆盖同意拒绝、身份缺失、费用上限、长度上限和敏感字段脱敏。

### 当前边界

- GatewayPolicy 当前位于 Android 端前置层，尚未替代服务端 Gateway；生产 APK 仍不得内置长期主 Key。
- 超时参数已进入策略契约，真实网络重试、限流、配额和服务端审计待后续联调。

### 验证结果

- testDebugUnitTest：通过。
- assembleDebug：通过。
- connectedDebugAndroidTest：通过，Pixel_Tablet（Android 15）19 项全部通过。
